
# #######################################################################################
# AUTOGENERATED HYPR CONFIG.
# PLEASE USE THE CONFIG PROVIDED IN THE GIT REPO /examples/hypr.conf AND EDIT IT,
# OR EDIT THIS ONE ACCORDING TO THE WIKI INSTRUCTIONS.
# #######################################################################################

#
# Please note not all available settings / options are set here.
# For a full list, see the wiki
#

# See https://wiki.hyprland.org/Configuring/Monitors/
monitor=,1920x1080,auto,1.25


# See https://wiki.hyprland.org/Configuring/Keywords/ for more

# Execute your favorite apps at launch
exec-once = [[ $(cat /sys/class/power_supply/BAT0/status) == "Discharging" ]] && bluetoothctl power off
exec-once = waylock -init-color 0x000000
exec-once = waybar
exec-once = hyprpaper
exec-once = ~/randomWall.sh
exec-once = wl-paste --watch cliphist store    
exec-once = rm "$HOME/.cache/cliphist/db"
exec-once = [workspace special:term silent] alacritty --class scratch -e tmux new-session -A -s "scratch"
exec-once = nm-applet --no-agent
exec-once = ~/scripts/blu-notif
exec-once = /usr/lib/polkit-gnome/polkit-gnome-authentication-agent-1
exec-once = ~/scripts/bat-notif.sh
exec-once = wlsunset

# Source a file (multi-file configs)
source = ~/.config/hypr/mocha.conf

# Set programs that you use
$terminal = alacritty
$fileManager = thunar
$menu = wofi --show drun

# Some default env vars.
#
env = HYPRCURSOR_THEME,Bibata-Classic
env = HYPRCURSOR_SIZE,24
# env = XCURSOR_SIZE,24
env = QT_QPA_PLATFORMTHEME,qt5ct # change to qt6ct if you have that
# env = WLR_DRM_DEVICES,/dev/dri/card0

# For all categories, see https://wiki.hyprland.org/Configuring/Variables/
input {
    kb_layout = us
    kb_variant =
    kb_model =
    kb_options =
    kb_rules =

    follow_mouse = 1

    touchpad {
        natural_scroll = no
        disable_while_typing = yes
    }

    sensitivity = 0 # -1.0 to 1.0, 0 means no modification.
    repeat_rate = 30
    repeat_delay = 270
}

general {
    # See https://wiki.hyprland.org/Configuring/Variables/ for more

    gaps_in = 3
    gaps_out = 0
    border_size = 2
    # col.active_border = rgba(33ccffee) rgba(00ff99ee) 45deg
    # col.inactive_border = rgba(595959aa)
    col.active_border = $mauve $blue 45deg
    col.inactive_border = rgba(595959aa)
    layout = dwindle
    # Please see https://wiki.hyprland.org/Configuring/Tearing/ before you turn this on
    allow_tearing = false
    # no_cursor_warps = true
    cursor_inactive_timeout = 2
}

binds { 
    allow_workspace_cycles = true
}

decoration {
    # See https://wiki.hyprland.org/Configuring/Variables/ for more

    rounding = 10
    
    blur {
        enabled = true
        size = 4
        passes = 3
        new_optimizations = true
        # special = true
    }

    drop_shadow = yes
    shadow_range = 4
    shadow_render_power = 3
    col.shadow = rgba(1a1a1aee)
    dim_special = 0.1
    inactive_opacity = 0.9
    # dim_inactive = true
    # dim_strength = 0.1
    layerrule = blur, wofi
}

animations {
    enabled = yes

    # Some default animations, see https://wiki.hyprland.org/Configuring/Animations/ for more

    bezier = myBezier, 0.05, 0.9, 0.1, 1.05

    animation = windows, 1, 4, myBezier
    animation = windowsOut, 1, 4, default, popin 80%
    animation = border, 1, 4, default
    animation = borderangle, 1, 4, default
    animation = fade, 1, 4, default
    animation = workspaces, 1, 4, default
    animation = specialWorkspace, 1, 4, default, slidefadevert
}

dwindle {
    # See https://wiki.hyprland.org/Configuring/Dwindle-Layout/ for more
    pseudotile = yes # master switch for pseudotiling. Enabling is bound to mainMod + P in the keybinds section below
    preserve_split = yes # you probably want this
    # no_gaps_when_only = 1 
    force_split = 2
}

master {
    # See https://wiki.hyprland.org/Configuring/Master-Layout/ for more
    new_is_master = true
    # no_gaps_when_only = 1 
}

gestures {
    # See https://wiki.hyprland.org/Configuring/Variables/ for more
    workspace_swipe = off
}

misc {
    # See https://wiki.hyprland.org/Configuring/Variables/ for more
    force_default_wallpaper = -1 # Set to 0 or 1 to disable the anime mascot wallpapers
    disable_splash_rendering = true
}

# Example per-device config
# See https://wiki.hyprland.org/Configuring/Keywords/#per-device-input-configs for more
device {
    name = epic-mouse-v1
    sensitivity = -0.5
}

# Example windowrule v1
# windowrule = float, ^(kitty)$
# Example windowrule v2
# windowrulev2 = float,class:^(kitty)$,title:^(kitty)$
# See https://wiki.hyprland.org/Configuring/Window-Rules/ for more
windowrulev2 = suppressevent maximize, class:.* # You'll probably like this.
workspace=s[true],gapsin:3,gapsout:80 150 80 150
windowrulev2 = noborder, initalclass:^(thorium-browser)$

# See https://wiki.hyprland.org/Configuring/Keywords/ for more
$mainMod = ALT

# Example binds, see https://wiki.hyprland.org/Configuring/Binds/ for more
bind = $mainMod, RETURN, exec, $terminal
bind = $mainMod, M, exit, 
# bind = $mainMod, E, exec, $fileManager
bind = $mainMod SHIFT, SPACE, togglefloating, 
bind = $mainMod, D, exec, $menu
bind = $mainMod SHIFT, D, exec, $menu --show run
bind = $mainMod, P, pseudo, # dwindle
bind = $mainMod, E, togglesplit, # dwindle
bind = $mainMod, F, fullscreen, # dwindle

# Move focus with mainMod + arrow keys
bind = $mainMod, h, movefocus, l
bind = $mainMod, l, movefocus, r
bind = $mainMod, k, movefocus, u
bind = $mainMod, j, movefocus, d
bind = $mainMod, left, movefocus, l
bind = $mainMod, right, movefocus, r
bind = $mainMod, up, movefocus, u
bind = $mainMod, down, movefocus, d

bind = $mainMod SHIFT, H, movewindow, l
bind = $mainMod SHIFT, L, movewindow, r
bind = $mainMod SHIFT, K, movewindow, u
bind = $mainMod SHIFT, J, movewindow, d

#resize
bind = ALT, R, submap, Resize
submap = Resize
binde = , l, resizeactive, 10 0
binde = , h, resizeactive, -10 0
binde = , k, resizeactive, 0 -10
binde = , j, resizeactive, 0 10
bind = , escape, submap, reset 
submap = reset

#Power
bind = ALT SHIFT, E, submap, (S)hutdown (R)eboot (L)ogout Sus(p)end
submap = (S)hutdown (R)eboot (L)ogout Sus(p)end
binde = $mainMod, S, exec, systemctl poweroff
binde = $mainMod, R, exec, systemctl reboot
binde = $mainMod, L, exec, logout
binde = $mainMod, P, exec, systemctl suspend
bind = , escape, submap, reset 
submap = reset

# Switch workspaces with mainMod + [0-9]
bind = $mainMod, 1, workspace, 1
bind = $mainMod, 2, workspace, 2
bind = $mainMod, 3, workspace, 3
bind = $mainMod, 4, workspace, 4
bind = $mainMod, 5, workspace, 5
bind = $mainMod, 6, workspace, 6
bind = $mainMod, 7, workspace, 7
bind = $mainMod, 8, workspace, 8
bind = $mainMod, 9, workspace, 9
bind = $mainMod, 0, workspace, 10
bind = $mainMod, TAB, workspace, previous

# Move active window to a workspace with mainMod + SHIFT + [0-9]
bind = $mainMod SHIFT, 1, movetoworkspacesilent, 1
bind = $mainMod SHIFT, 2, movetoworkspacesilent, 2
bind = $mainMod SHIFT, 3, movetoworkspacesilent, 3
bind = $mainMod SHIFT, 4, movetoworkspacesilent, 4
bind = $mainMod SHIFT, 5, movetoworkspacesilent, 5
bind = $mainMod SHIFT, 6, movetoworkspacesilent, 6
bind = $mainMod SHIFT, 7, movetoworkspacesilent, 7
bind = $mainMod SHIFT, 8, movetoworkspacesilent, 8
bind = $mainMod SHIFT, 9, movetoworkspacesilent, 9
bind = $mainMod SHIFT, 0, movetoworkspacesilent, 10

# Example special workspace (scratchpad)
bind = $mainMod, SPACE, togglespecialworkspace, term
# bind = $mainMod SHIFT, S, movetoworkspace, special:magic

# Scroll through existing workspaces with mainMod + scroll
bind = $mainMod, mouse_down, workspace, e+1
bind = $mainMod, mouse_up, workspace, e-1

# Move/resize windows with mainMod + LMB/RMB and dragging
bindm = $mainMod, mouse:272, movewindow
bindm = $mainMod, mouse:273, resizewindow

binde = ,XF86MonBrightnessUp, exec, brightnessctl -c 'backlight' s +2% && notify-send --hint=string:x-dunst-stack-tag:brtns -t 1000 "Brightness" $(brightnessctl | awk '/Current brightness:/ {print $4}')
binde = ,XF86MonBrightnessDown, exec, brightnessctl -c 'backlight' s 2%- && notify-send --hint=string:x-dunst-stack-tag:brtns -t 1000 "Brightness" $(brightnessctl | awk '/Current brightness:/ {print $4}')  
binde = $mainMod, q, killactive,
binde = ,XF86AudioRaiseVolume, exec, pamixer -i  5 --allow-boost && pkill -RTMIN+10 waybar
binde = ,XF86AudioLowerVolume, exec, pamixer -d  5 --allow-boost && pkill -RTMIN+10 waybar
binde = ,XF86AudioMute, exec, pamixer -t && pkill -RTMIN+10 waybar
binde = ,XF86AudioMicMute, exec, pamixer --default-source -t

bind = ALT SHIFT, X, exec, waylock -init-color 0x000000

bind = $mainMod, V, exec, cliphist list | wofi --dmenu | cliphist decode | wl-copy

bind = SUPER, B, exec, ~/scripts/blu f

#Screenshots
bind = $mainMod SHIFT, S, exec, ~/scripts/grimblast copy area && notify-send -t 1000 "SS Copied" 
bind = SUPER SHIFT, S, exec, ~/scripts/grimblast save area && notify-send -t 1000 "SS saved" 
bind = SUPER, Print, exec, ~/scripts/grimblast save screen && notify-send -t 1000 "SS saved" 
bind = ,Print, exec, ~/scripts/grimblast copy screen && notify-send -t 1000 "SS Copied" 

#OCR
bind = $mainMod, O, exec, grimblast save area -| tesseract stdin - | wl-copy && notify-send "Copied Text" "$(wl-paste | tr '\n' ' ')"

bind = SUPER, w, exec, killall waybar;waybar

bind = SUPER, R, exec, ~/randomWall.sh

bind = SUPER, T, exec, hyprctl keyword "device[asue1213:00-04f3:3294-touchpad]:enabled" 1
bind = SUPER, SPACE, exec, hyprctl keyword "device[asue1213:00-04f3:3294-touchpad]:enabled" 0

bind = CONTROL SHIFT, SPACE, exec, dunstctl close-all
